fn func(num:int)->int {
    while (!((a+b)<c)) {
        a = b + c;
    }
    if (a==b) {
        ret c;
    }
    if (x >0) {
        if (y >0) {
            x = x + y;
        }
        else {
            x = x - y;
        }
    } else {
        ;
    }
    ret 0;
}

struct MyStruct {
    node:int,
    len:int
}

let number:int = 7;
let x:MyStruct;

fn func(x:MyStruct) {
    x.node = 3;
    x.len = 8;
}